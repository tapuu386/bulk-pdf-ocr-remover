# bulk-pdf-ocr-remover
A specialized C# .NET automation tool that recursively processes folders to remove OCR layers from PDFs, converting them into non-searchable images while perfectly preserving the original directory hierarchy.
